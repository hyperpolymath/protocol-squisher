// Test HashMap with match
fn main() -> i32 {
    let map = hashmap_new();
    let map = hashmap_insert(map, "name", "Alice");

    let result = match hashmap_get(&map, "name") {
        Some(val) => {
            print(val);
            0
        }
        None => 1
    };

    result
}
