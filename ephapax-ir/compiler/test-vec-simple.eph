// Simple Vec test
fn main() -> i32 {
    let v1 = vec_new();
    let v2 = vec_push(v1, 10);
    let v3 = vec_push(v2, 20);

    let len = vec_length(&v3);
    print(len);  // Should print 2

    match vec_get(&v3, 0) {
        Some(val) => {
            print(val);  // Should print 10
            0
        }
        None => 1
    }
}
