// Test complete Result pattern matching
fn validate_positive(x: i32) -> Result<i32, String> {
    if x > 0 {
        Ok(x)
    } else {
        Err("negative")
    }
}

fn main() -> i32 {
    let result = validate_positive(42);
    match result {
        Ok(n) => n,
        Err(msg) => 0
    }
}
