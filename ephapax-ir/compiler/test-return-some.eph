// Just return Some without pattern matching
fn get_value() -> Option<i32> {
    Some(42)
}

fn main() -> i32 {
    let val = get_value();
    // Can't use val here due to linear types, but that's OK for this test
    42
}
