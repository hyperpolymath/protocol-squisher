// Test Result Err case
fn validate_positive(x: i32) -> Result<i32, String> {
    if x > 0 {
        Ok(x)
    } else {
        Err("negative value")
    }
}

fn main() -> i32 {
    let result = validate_positive(0);
    match result {
        Ok(n) => n,
        Err(msg) => 99
    }
}
