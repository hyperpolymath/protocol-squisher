// Debugging HashMap usage
fn main() -> i32 {
    let map = hashmap_new();
    let map = hashmap_insert(map, "name", "Alice");
    let has_name = hashmap_contains_key(&map, "name");
    let has_role = hashmap_contains_key(&map, "role");
    if has_name {
        0
    } else {
        1
    }
}
