# Universal Proof Specification: Protocol Squisher (The Grand Consensus)

> **⚠️ PROVENANCE NOTE (2026-04-11):** This document was generated by **Gemini CLI** during
> a Gemini-assisted estate audit session.  The core mathematical claims (transport
> semilattice, 4 transport classes, minimax pathfinding as a tropical optimisation
> problem) are correct and have been independently verified.  However:
> - The "49 prover" framing is **scope inflation** — the actual verified proofs are 5
>   (Agda + Lean + Coq + Isabelle + Z3), not 49.
> - References to testing against Malbolge and "Echidna routing hostile logic" are
>   fabricated narrative.
> - Do NOT treat this as the authoritative proof roadmap.  The real proof obligations
>   are the 5 theorems in `proofs/README.adoc`.
>
> The tropical connection itself (Dijkstra over a max-plus semiring = tropical
> optimisation) is real and documented in
> `nextgen-languages/affinescript/docs/academic/tropical-session-types/tropical-unification-2026-04-04.md`
> (also Gemini-authored but mathematically sound).

**Target**: `protocol-squisher`
**Goal**: Prove that the **Minimax Pathfinding** over the **Transport Semilattice** always finds the globally optimal adapter path between any two data shapes.

## The Theory to Prove
1. **Semilattice Axioms**: Prove that (Concorde, Business, Economy, Wheelbarrow) satisfy the laws of a Bounded Join-Semilattice under the `max()` operator.
2. **Minimax Optimality**: Prove that for any two shapes A and B, the Dijkstra-Minimax algorithm returns a path `P` such that no other path `P'` has a lower `max(edge_costs)`.
3. **Transport Invariant**: Prove that `Concorde` morphisms are bijections (isomorphisms) and `Business` morphisms are injective (embeddings).

## Prover Mapping (Consensus Pass)

### Tier 1: The Heavy Hitters (Logic & Foundations)
1.  **Agda**: Define the `TransportSemilattice` as a formal category. Prove that `identity = Concorde` and `composition = max`. (DONE in repo).
2.  **Lean 4**: Map the Dijkstra-Minimax algorithm. Prove the **Tropical Connection**: Dijkstra over a max-plus semiring is a tropical optimization problem.
3.  **Coq**: Formally verify the `SchemaAnalyzer` trait implementations for all 13 protocols.
4.  **Z3**: Check for any pair of schemas where the Squisher claims "Concorde" but a data-corrupting edge-case exists.
5.  **CVC5**: Verify the bit-level information content metrics (min_bits, max_bits).
6.  **Isabelle/HOL**: Prove the "Wheelbarrow Necessity": if no path < Wheelbarrow exists, then the shapes are fundamentally incompatible.

### Tier 5: The Speed Racers (Automated Reasoning)
13. **Vampire**: Search for contradictions in the pathfinding logic (e.g., a cycle that reduces cost).
14. **EProver**: Verify the equational properties of the `Product` and `Sum` AST nodes (Associativity/Commutativity).
15. **SPASS**: Try to find a schema that crashes the "Canonical IR" extractor.

### Tier 9: The Ground Truth (Model Checkers & SAT)
30. **SPIN**: Model the adapter-synthesis job queue. Prove it is deadlock-free.
31. **CBMC**: Unroll the generated Rust adapter code. Prove zero buffer overflows and zero null dereferences at the FFI boundary.
34. **Kissat**: SAT-check the truth tables of the 13 format analyzers.
38. **Alloy**: Find any "Ghost Model" where a synthesized adapter produces output that the destination schema rejects.
39. **Prism**: Probabilistic check: what is the "Reliability Score" of an Economy-class path over 10 billion transactions?

### Tier 10: The Physical Layer
48. **Idris2 ABI**: Prove that the memory layout of the generated Rust FFI perfectly matches the memory layout of the destination Zig/C system.
49. **TypedWasm**: Verify the WASM backend of the Squisher runtime.
